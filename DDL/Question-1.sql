-- Write an SQL query to create a table named "Employees" with columns for "EmployeeID", "FirstName", "LastName", and "Salary":

CREATE DATABASE COMPANY;

USE COMPANY;

create table Employees (
   EmployeeID INT PRIMARY KEY,
   firstName varchar(50),
   LastName varchar(50),
   salary int
   );
   

